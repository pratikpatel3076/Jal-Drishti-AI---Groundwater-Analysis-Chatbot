# OpenAI API key — required for AI-powered responses
# Without this, the backend falls back to TF-IDF matching
OPENAI_API_KEY=sk-your_key_here

# Default language (EN, HI, BN, TE, TA, GU)
DEFAULT_LANGUAGE=EN
